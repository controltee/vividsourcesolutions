# Dependencies (image pipeline only — never runs on the server)
node_modules/

# Secrets — never commit
.env
.env.*

# Image pipeline working dirs
scripts/out/
scripts/raw/

# Reconnaissance output from scan-posters.mjs. Regenerated on demand and it
# lists absolute paths on whichever machine ran it, so it is never source.
scripts/scan-result.json

# Vercel
.vercel/

# OS cruft
.DS_Store
Thumbs.db
